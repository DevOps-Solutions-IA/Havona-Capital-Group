import {Controller,Get} from '@nestjs/common';import {RequirePermissions} from './decorators';import {PrismaService} from './prisma.service';
@Controller('dashboard') export class DashboardController {constructor(private readonly db:PrismaService){}@Get('summary') @RequirePermissions('users.read') async summary(){const now=new Date();const [users,activeUsers,activeSessions]=await this.db.$transaction([this.db.user.count(),this.db.user.count({where:{isActive:true}}),this.db.session.count({where:{revokedAt:null,expiresAt:{gt:now}}})]);return {users,activeUsers,activeSessions,generatedAt:now.toISOString()}}}
