'use client'; import {Card,Badge} from '@havona/ui';import {PageHeader} from '@/components/page';import {useAuth} from '@/lib/auth'; export default function Page(){const{user}=useAuth();if(!user)return null;return <><PageHeader title="Mi perfil" description="Datos asociados a tu sesión actual."/><Card className="max-w-2xl"><dl className="grid gap-5 sm:grid-cols-2"><Item label="Nombre" value={user.name}/><Item label="Correo" value={user.email}/><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Estado</dt><dd className="mt-2"><Badge tone={user.isActive?'success':'danger'}>{user.isActive?'Activo':'Inactivo'}</Badge></dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">Roles</dt><dd className="mt-2 flex flex-wrap gap-2">{user.roles.map(r=><Badge key={typeof r==='string'?r:r.id} tone="info">{typeof r==='string'?r:r.name}</Badge>)}</dd></div></dl></Card></>};function Item({label,value}:{label:string;value:string}){return <div><dt className="text-xs font-semibold uppercase tracking-wide text-slate-500">{label}</dt><dd className="mt-1 text-slate-900">{value}</dd></div>}
