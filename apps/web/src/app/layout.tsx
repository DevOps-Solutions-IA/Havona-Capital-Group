import type { Metadata } from 'next';
import { Inter, Outfit } from 'next/font/google';
import './globals.css';
import {GlobalHenryLayer} from '@/components/henry/global-henry-layer';
const inter=Inter({subsets:['latin'],variable:'--font-inter'}); const outfit=Outfit({subsets:['latin'],variable:'--font-outfit'});
const siteUrl=process.env.NEXT_PUBLIC_SITE_URL||'http://localhost:3000';
export const metadata:Metadata={metadataBase:new URL(siteUrl),title:{default:'HAVONA CAPITAL GROUP | Consultoría patrimonial',template:'%s | HAVONA CAPITAL GROUP'},description:'Consultoría patrimonial, protección financiera y tecnología para personas, familias y empresas.',applicationName:'HAVONA CAPITAL GROUP',authors:[{name:'HAVONA CAPITAL GROUP'}],openGraph:{type:'website',locale:'es_CO',siteName:'HAVONA CAPITAL GROUP',title:'HAVONA CAPITAL GROUP | Consultoría patrimonial',description:'Protegemos su presente. Construimos su patrimonio.'},twitter:{card:'summary_large_image',title:'HAVONA CAPITAL GROUP | Consultoría patrimonial',description:'Protegemos su presente. Construimos su patrimonio.'},robots:{index:true,follow:true}};
export default function RootLayout({children}:{children:React.ReactNode}){const organization={'@context':'https://schema.org','@type':'Organization',name:'HAVONA CAPITAL GROUP',url:siteUrl,description:'Consultoría patrimonial, protección financiera y tecnología para personas, familias y empresas.'};return <html lang="es"><body className={`${inter.variable} ${outfit.variable} font-sans antialiased`}>{children}<GlobalHenryLayer/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(organization).replace(/</g,'\\u003c')}}/></body></html>}
